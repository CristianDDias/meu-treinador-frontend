import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  title: {
    width: '100%',
    color: '#ffffff',
  },
  button: {
    position: 'absolute',
    color: '#ffffff',
  },
  hidden: {
    display: 'none',
  },
});
